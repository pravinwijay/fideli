import SwiftUI

struct AddCardView: View {
    var body: some View {
        NavigationStack {
            Text("L'écran pour scanner arrivera ici.")
                .navigationTitle("Ajouter une carte")
        }
    }
}
